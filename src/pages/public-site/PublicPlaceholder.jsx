import { PlaceholderPage } from "@/pages/PlaceholderPage";

export default function PublicPlaceholder(props) {
  return (
    <PlaceholderPage
      section="Public Site Placeholder"
      title="Public Website"
      description="Public routes exist now only as placeholders. The public website will be designed near the end."
      {...props}
    />
  );
}
